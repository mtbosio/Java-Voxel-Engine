package worldGen;

public class ChunkManager {
    /*private List<Chunk> chunks; // List to hold all chunks
    private main.Renderer renderer;

    public WorldGen.ChunkManager(main.Renderer renderer) {
        this.renderer = renderer;
        this.chunks = new ArrayList<>();
    }

    // updates all chunk lists
    private void update(){

    }

    public void addChunk(float x, float y, float z) {
        Chunk chunk = new Chunk(renderer, x, y, z);
        chunks.add(chunk);
    }

    public void removeChunk(Chunk chunk) {
        chunks.remove(chunk);
    }

    public void renderChunks() {
        for (Chunk chunk : chunks) {
            if (chunk.isVisible()) { // Check if the chunk is visible
                chunk.renderChunk(); // Delegate rendering to the chunk
            }
        }
    }

    public void updateChunks() {
        // Add logic here if you need to update chunk states (e.g., loading/unloading chunks)
        for (Chunk chunk : chunks) {
            // Potential update logic
        }
    }

    public List<Chunk> getChunks() {
        return chunks;
   }*/
}
